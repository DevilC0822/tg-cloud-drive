# 当前 docker-compose 默认可直接启动，通常无需配置环境变量。
# 首次启动时在 Web 初始化页面填写：
# - Telegram Bot Token
# - 存储 Chat ID（-100xxxxxxxxxx 或 @channelusername）
# - 管理员密码

# 可选：固定 Cookie 签名密钥（base64），用于重启后保持登录态
# COOKIE_SECRET_B64=

# 可选：分享链接固定基地址（默认从请求头推断）
# BASE_URL=

# 可选：从自定义请求头读取公开访问基地址（优先级高于 X-Forwarded-*）
# PUBLIC_URL_HEADER=

# 可选：启用后拒绝通过 IP:PORT 直接访问（建议生产环境配合域名反代启用）
# DISABLE_IP_PORT_ACCESS=false

# 可选：后端写入自建 Bot API 凭据文件目录（默认 /var/lib/tgcd-runtime/self-hosted-bot-api）
# SELF_HOSTED_BOT_API_SECRET_DIR=

# 可选：自建 Bot API 本地文件直传目录（默认 /var/lib/tgcd-runtime/self-hosted-bot-api-upload）
# SELF_HOSTED_BOT_API_UPLOAD_DIR=

# 可选：是否启用 Torrent URL/种子任务（默认 true）
# TORRENT_ENABLED=true

# 可选：Torrent 元文件工作目录（默认 /var/lib/tgcd-runtime/torrents）
# TORRENT_WORK_DIR=

# 可选：下载器与后端共享的下载目录（默认 /var/lib/tgcd-torrent-data）
# TORRENT_DOWNLOAD_DIR=

# 可选：下载器 qBittorrent WebAPI 地址（默认 http://qbittorrent:8080）
# TORRENT_QBT_BASE_URL=

# 可选：qBittorrent 登录用户名/密码（默认 admin/adminadmin）
# TORRENT_QBT_USERNAME=
# TORRENT_QBT_PASSWORD=

# 可选：私有 PT 模式建议关闭 DHT/PEX/LSD（默认 false）
# TORRENT_QBT_DISABLE_DHT_PEX_LSD=false

# 可选：任务完成后自动删除 qBittorrent 内任务与下载文件（默认 true）
# TORRENT_QBT_DELETE_ON_COMPLETE=true

# 可选：仅允许 private torrent（默认 false）
# TORRENT_REQUIRE_PRIVATE=false

# 可选：允许的 announce 域名白名单（逗号分隔），留空不限制
# TORRENT_ALLOWED_ANNOUNCE_DOMAINS=
